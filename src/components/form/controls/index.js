import {
  Enum,
  Number,
  String,
  Boolean,
  Array,
  Object,
} from "./JSONSchemaTypes.jsx"

export const controls = {
  enum: Enum,
  number: Number,
  string: String,
  boolean: Boolean,
  array: Array,
  object: Object,
}
